# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title_de: PBS MiData - Dokumentation
desc_de: Offizielle Dokumentation für die MiData der PBS

title_en: PBS MiData - Documentation
desc_en: Official documentation for the MiData of PBS

title_fr: PBS MiData - Documentation
desc_fr: Documentation officielle pour les MiData du PBS

title_it: PBS MiData - Documentazione
desc_it: Documentazione ufficiale per i MiData di PBS

menuItem_1_de: Einstieg
menuItem_2_de: Handbuch
menuItem_3_de: FAQ

menuItem_1_en: Introduction
menuItem_2_en: Manual
menuItem_3_en: FAQ

menuItem_1_fr: Initiation
menuItem_2_fr: Manuel
menuItem_3_fr: FAQ

menuItem_1_it: Iscrizione
menuItem_2_it: Manuale
menuItem_3_it: FAQ

# baseurl: ''
# url: "" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown

# plugins:

# You need to have this extra permalink_custom_vars array to tell the plugin which substitutions to make
permalink_custom_vars: ['lang']

permalink: none

sass:
  # loading path from site root
  # default to _sass
  # sass_dir: _sass

  # style : nested (default), compact, compressed, expanded
  #         :nested, :compact, :compressed, :expanded also works
  # see http://sass-lang.com/documentation/file.SASS_REFERENCE.html#output_style
  # on a typical twitter bootstrap stats are :
  # nested 138,7kB, compact 129,1kB, expanded 135,9 kB, compressed 122,4 kB
  style: compressed

# defaults:
#   -
#     scope:
#       path: '_posts'
#     values:
#       layout: 'tool'

collections:
  faqs:
    output: true
  articles:
    output: true
  introductions:
    output: true

defaults:
  -
    scope:
      path: ""
    values:
      layout: default
  -
    scope:
      path: "_faqs"
    values:
      permalink: "/:lang/faq/:slug"


# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - package.json
  - yarn.lock
